export default function DemoNotice() {
  return (
    <p className="mt-4 rounded-chip bg-indigo-100/60 px-4 py-3 text-[14px] text-indigo-700">
      This is the pre-computed sample result for the hero scenario (₹60,000 spare, credit card
      at 42% APR). The live engine was still waking — your own numbers will run once it&rsquo;s up.
    </p>
  );
}
